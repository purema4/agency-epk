# Pinned to tested versions so an image rebuild can't silently pull a new release.
# Dependabot (.github/dependabot.yml) proposes upgrades as PRs, which CI tests before merge.
fastapi==0.141.1
uvicorn[standard]==0.53.0
httpx==0.28.1
pydantic-settings==2.15.0
